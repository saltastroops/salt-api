@Library('saao-shared-library') _

pipeline {
  agent any

  stages {
    agent {
      dockerfile {
        filename 'Dockerfile2'
        dir 'jenkins'
        args '-v saltapi-pipcache:/root/.cache -u 0:0'
      }
    }

    stage('Perform linting tests') {
      steps {
        echo 'Linting...'
      }
    }
  }
}
